#!/bin/sh

# Typecheck the active Next.js app before pushing.
# Artifacts (Replit scaffolding) are excluded — they are not deployed.
# Python mypy runs in CI (ci.yml), not here.
echo "Running pre-push typecheck on apps/web..."
pnpm --filter @luxai/web --if-present run typecheck
