# Path: packages/backtest/__init__.py
# Security: No credentials stored here. Backtest reads market data only — no order submission.
# Scale: Single-tenant. Run as a one-off script, not embedded in the FastAPI server.
